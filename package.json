{
  "name": "motoscrape",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx ./src/index.ts",
    "lint": "oxlint --fix .",
    "format": "prettier --write --experimental-cli .",
    "typecheck": "tsc --noEmit",
    "postinstall:playwright": "pnpm exec playwright install chromium",
    "postinstall": "pnpm run postinstall:playwright"
  },
  "dependencies": {
    "playwright": "^1.54.1",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@prettier/plugin-oxc": "latest",
    "@types/node": "latest",
    "@typescript/native-preview": "latest",
    "oxlint": "latest",
    "prettier": "latest",
    "tsx": "^4.20.3",
    "typescript": "latest"
  }
}
